# https://docs.readthedocs.com/platform/stable/config-file/v2.html
version: 2

build:
  os: ubuntu-26.04
  tools:
    python: "3.14"

python:
  install:
    - method: uv
      command: sync
      groups: all

sphinx:
  configuration: docs/conf.py
